export const REGISTRATION_REGISTER = 'REGISTRATION_REGISTER';

/**
 * The defaults that we use as long as the code hasn't sent anything.
 */
export default {
	[REGISTRATION_REGISTER]: () => '' // initial value of payload
};
